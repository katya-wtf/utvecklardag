extends ../layouts/default

block content
	.container: .row: .col-sm-10: .col-sm-offset-1: .col-md-8: col-md-offset-2
		article
			if !data.abouts
				Nothing to see here...
			if data.abouts
				h2= data.abouts[0].title
				p= data.abouts[0].content
